<UserControl x:Class="ProgramZaRacunovodstvo.Views.KreirajNabavku"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:ProgramZaRacunovodstvo.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Kreiraj dokument"
                   FontFamily="pack://application:,,,/Fonts/#Poppins Light"
                   FontSize="28"
                   Margin="20,40,0,20"
                   Width="759"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Top" />
        <ui:TextBox Grid.Row="1"
                    MinHeight="40"
                    MinWidth="220"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Margin="20"
                    PlaceholderText="Broj fakture"></ui:TextBox>
        <Border BorderBrush="LightGray"
                BorderThickness="1"
                Height="40"
                Width="220"
                CornerRadius="10"
                Padding="5"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Grid.Row="1"
                Margin="20,70,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Name="DatumTextbox"
                           Text="Datum slanja"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Left"
                           Margin="0,0,5,0"
                           Grid.Column="0" />

                <ui:CalendarDatePicker Name="DatumPocetni"
                                       Height="40"
                                       Width="40"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center"
                                       Margin="0"
                                       Grid.Column="1"
                                       Date="{Binding Date, Mode=TwoWay}">
                </ui:CalendarDatePicker>
                <Image Source="/slike/kalendar.png"
                       HorizontalAlignment="Right"
                       Width="12"
                       Height="12"
                       VerticalAlignment="Center"
                       IsHitTestVisible="False"
                       Grid.Column="1"
                       Panel.ZIndex="10"
                       Margin="2">
                    <Image.RenderTransform>
                        <ScaleTransform ScaleX="-1" />
                    </Image.RenderTransform>
                </Image>
            </Grid>
        </Border>
        <CheckBox Grid.Row="1" Margin="9,120,0,0" HorizontalAlignment="Left" VerticalAlignment="Top">Plaćeno</CheckBox>
        <ComboBox Text="Kupac"
                  Grid.Column="0" Grid.Row="1"
                  Height="40"
                  MinWidth="220"
                  VerticalAlignment="Top"
                  HorizontalAlignment="Left"
                  Margin="280,20,0,0">
            <ComboBoxItem IsSelected="True"
                          IsEnabled="False"
                          Content="Kupac" />
            <ComboBoxItem Content="Option 1" />
            <ComboBoxItem Content="Option 2" />
            <ComboBoxItem Content="Option 3" />
        </ComboBox>
        <Button Grid.Column="0"
                Grid.Row="1"
                Width="220"
                Height="40"
                VerticalAlignment="Top"
                HorizontalAlignment="Left"
                Margin="280,70,20,20"
                BorderThickness="0"
                Foreground="White"
                Content="Dodaj dokumente"
                FontFamily="pack://application:,,,/Fonts/#Poppins Light"
                FontSize="15">
            <Button.Effect>
                <DropShadowEffect ShadowDepth="10"
                                  Direction="320"
                                  Color="Gray"
                                  Opacity="0.5"
                                  BlurRadius="30" />
            </Button.Effect>
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Background"
                            Value="#FF4F72FF" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="13">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver"
                                             Value="True">
                                        <Setter Property="Background"
                                                Value="#82A6FF" />
                                    </Trigger>
                                    <Trigger Property="IsPressed"
                                             Value="True">
                                        <Setter Property="Background"
                                                Value="#3D64C2" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>
        <Border CornerRadius="20"
                Padding="0"
                Background="White"
                Grid.Row="1"
                Margin="20,200,20,60"
                Grid.ColumnSpan="2">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="10"
                                  Direction="320"
                                  Color="Gray"
                                  Opacity="0.5"
                                  BlurRadius="30" />
            </Border.Effect>
            <DataGrid Name="NabavkeDataGrid"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      Background="Transparent"
                      CanUserResizeRows="False"
                      GridLinesVisibility="None"
                      HeadersVisibility="Column"
                      BorderBrush="Transparent"
                      BorderThickness="0"
                      FontSize="16"
                      IsReadOnly="True"
                      FontFamily="pack://application:,,,/Fonts/#Poppins Light"
                      HorizontalGridLinesBrush="Transparent"
                      VerticalGridLinesBrush="Transparent"
                      ItemsSource="{Binding PagedNabavke}"
                      Width="auto"
                      Grid.Row="1"
                      Grid.Column="0"
                      HorizontalAlignment="Center"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      ScrollViewer.VerticalScrollBarVisibility="Disabled">

                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background"
                                Value="#FF4F72FF" />
                        <Setter Property="Foreground"
                                Value="White" />
                        <Setter Property="FontWeight"
                                Value="Bold" />
                        <Setter Property="Padding"
                                Value="10" />
                        <Setter Property="HorizontalContentAlignment"
                                Value="Center" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridColumnHeader">
                                    <Border Background="{TemplateBinding Background}"
                                            Padding="10"
                                            BorderThickness="0">
                                        <ContentPresenter HorizontalAlignment="Center" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <Style TargetType="DataGridRow">
                        <Setter Property="MinHeight"
                                Value="50" />
                        <Setter Property="Background"
                                Value="White" />
                        <Setter Property="BorderBrush"
                                Value="Transparent" />
                        <Setter Property="OverridesDefaultStyle"
                                Value="False" />

                    </Style>

                    <Style TargetType="DataGridCell">
                        <Setter Property="Background"
                                Value="Transparent" />
                        <Setter Property="BorderBrush"
                                Value="Transparent" />
                        <Style.Triggers>
                            <Trigger Property="IsSelected"
                                     Value="True">
                                <Setter Property="Background"
                                        Value="Transparent" />
                                <Setter Property="Foreground"
                                        Value="Black" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>

                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Šifra"
                                        Binding="{Binding BrojFakture}"
                                        Width="*"
                                        MinWidth="50">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextAlignment"
                                        Value="Center" />
                                <Setter Property="HorizontalAlignment"
                                        Value="Center" />
                                <Setter Property="Padding"
                                        Value="10" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Naziv"
                                        Binding="{Binding Status}"
                                        Width="*"
                                        MinWidth="80">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextAlignment"
                                        Value="Center" />
                                <Setter Property="HorizontalAlignment"
                                        Value="Center" />
                                <Setter Property="Padding"
                                        Value="10" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Količina"
                                        Binding="{Binding Dobavljac}"
                                        Width="*"
                                        MinWidth="80">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextAlignment"
                                        Value="Center" />
                                <Setter Property="HorizontalAlignment"
                                        Value="Center" />
                                <Setter Property="Padding"
                                        Value="10" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Cena"
                                        Binding="{Binding OsnovicaRSD}"
                                        Width="*"
                                        MinWidth="80">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextAlignment"
                                        Value="Center" />
                                <Setter Property="HorizontalAlignment"
                                        Value="Center" />
                                <Setter Property="Padding"
                                        Value="10" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Iznos bez PDV"
                                        Binding="{Binding PdvRSD}"
                                        Width="*"
                                        MinWidth="80">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextAlignment"
                                        Value="Center" />
                                <Setter Property="HorizontalAlignment"
                                        Value="Center" />
                                <Setter Property="Padding"
                                        Value="10" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="PDV %"
                                        Binding="{Binding UkupnoRSD}"
                                        Width="*"
                                        MinWidth="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextAlignment"
                                        Value="Center" />
                                <Setter Property="HorizontalAlignment"
                                        Value="Center" />
                                <Setter Property="Padding"
                                        Value="10" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Iznos sa PDV"
                                        Binding="{Binding DatumSlanja, StringFormat=\{0:dd.MM.yyyy\}}"
                                        Width="*"
                                        MinWidth="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextAlignment"
                                        Value="Center" />
                                <Setter Property="HorizontalAlignment"
                                        Value="Center" />
                                <Setter Property="Padding"
                                        Value="10" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTemplateColumn Header="Akcije"
                                            Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal"
                                            HorizontalAlignment="Center">
                                    <Button Command="{Binding Izbrisi}"
                                            Width="40"
                                            Height="30"
                                            BorderThickness="0"
                                            Foreground="White"
                                            Cursor="Hand"
                                            Margin="0,0,10,0">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Background"
                                                        Value="#F44336" />
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}"
                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                                    CornerRadius="5">
                                                                <ContentPresenter HorizontalAlignment="Center"
                                                                                  VerticalAlignment="Center" />
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver"
                                                                         Value="True">
                                                                    <Setter Property="Background"
                                                                            Value="#F88780" />
                                                                </Trigger>
                                                                <Trigger Property="IsPressed"
                                                                         Value="True">
                                                                    <Setter Property="Background"
                                                                            Value="#D32F2F" />
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver"
                                                             Value="True">
                                                        <Setter Property="Background"
                                                                Value="#F88780" />
                                                    </Trigger>
                                                    <Trigger Property="IsPressed"
                                                             Value="True">
                                                        <Setter Property="Background"
                                                                Value="#D32F2F" />
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                        <Image Source="/slike/izbrisi.png"
                                               Width="25"
                                               Height="20"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center" />
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
        <ui:TextBox Grid.Row="2"
                    Grid.ColumnSpan="2"
                    MinHeight="40"
                    MinWidth="90"
                    Width="90"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    PlaceholderText="Šifra"
                    Margin="20,0,0,0"></ui:TextBox>
        <ui:TextBox Grid.Row="2"
                    Grid.ColumnSpan="2"
                    MinHeight="40"
                    MinWidth="100"
                    Width="100"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    PlaceholderText="Naziv"
                    Margin="120,0,0,0"></ui:TextBox>
        <ui:TextBox Grid.Row="2"
                    Grid.ColumnSpan="2"
                    MinHeight="40"
                    MinWidth="71"
                    Width="71"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    PlaceholderText="Količina"
                    Margin="230,0,0,0"></ui:TextBox>
        <ui:TextBox Grid.Row="2"
                    Grid.ColumnSpan="2"
                    MinHeight="40"
                    MinWidth="90"
                    Width="90"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    PlaceholderText="Cena"
                    Margin="310,0,0,0"></ui:TextBox>
        <ui:TextBox Grid.Row="2"
                    Grid.ColumnSpan="2"
                    MinHeight="40"
                    MinWidth="111"
                    Width="111"
                    IsEnabled="False"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    PlaceholderText="Iznos bez PDV"
                    Margin="415,0,0,0"></ui:TextBox>
        <ui:TextBox Grid.Row="2"
                    Grid.ColumnSpan="2"
                    MinHeight="40"
                    MinWidth="66"
                    Width="66"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    PlaceholderText="PDV %"
                    Margin="540,0,0,0"></ui:TextBox>
        <ui:TextBox Grid.Row="2"
                    Grid.ColumnSpan="2"
                    MinHeight="40"
                    MinWidth="103"
                    Width="103"
                    IsEnabled="False"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    PlaceholderText="Iznos sa PDV"
                    Margin="615,0,0,0"></ui:TextBox>
        <Button Grid.ColumnSpan="2"
                Grid.Row="2"
                Width="160"
                Height="40"
                VerticalAlignment="Top"
                HorizontalAlignment="Left"
                Margin="730,0,20,0"
                BorderThickness="0"
                Foreground="White"
                Content="Dodaj novu stavku"
                FontFamily="pack://application:,,,/Fonts/#Poppins Light"
                FontSize="15">
            <Button.Effect>
                <DropShadowEffect ShadowDepth="10"
                                  Direction="320"
                                  Color="Gray"
                                  Opacity="0.5"
                                  BlurRadius="30" />
            </Button.Effect>
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Background"
                            Value="#FF4F72FF" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="13">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver"
                                             Value="True">
                                        <Setter Property="Background"
                                                Value="#82A6FF" />
                                    </Trigger>
                                    <Trigger Property="IsPressed"
                                             Value="True">
                                        <Setter Property="Background"
                                                Value="#3D64C2" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>
        <TextBlock FontFamily="pack://application:,,,/Fonts/#Poppins Light"
                   FontSize="23"
                   Text="Iznos pre PDV: 0 RSD" Grid.Row="2" Grid.ColumnSpan="2" Margin="20,80,0,0"></TextBlock>
        <TextBlock FontFamily="pack://application:,,,/Fonts/#Poppins Light"
                   FontSize="23"
                   Text="PDV iznos: 0 RSD"
                   Grid.Row="2"
                   Grid.ColumnSpan="2"
                   Margin="20,130,0,0"></TextBlock>
        <TextBlock FontFamily="pack://application:,,,/Fonts/#Poppins Light"
                   FontSize="23"
                   Text="Ukupan iznos: 0 RSD"
                   Grid.Row="2"
                   Grid.ColumnSpan="2"
                   Margin="20,180,0,0"></TextBlock>
    </Grid>
</UserControl>
