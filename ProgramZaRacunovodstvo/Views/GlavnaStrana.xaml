<UserControl x:Class="ProgramZaRacunovodstvo.ViewModel.GlavnaStrana"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProgramZaRacunovodstvo.ViewModels"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:vms="clr-namespace:ProgramZaRacunovodstvo.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <vms:Test/>
    </UserControl.DataContext>

    <Grid Background="White">
        <ScrollViewer>
            <UniformGrid Columns="2">

                <StackPanel>
                    <TextBlock Text="Dobrodošli ImeFirme" FontFamily="pack://application:,,,/Fonts/#Poppins Light" FontSize="28" Margin="25,20,0,5" Width="759"/>
                    <lvc:PieChart Series="{Binding Series2}" Title="{Binding Title}" LegendPosition="Bottom" Height="400"/>
                </StackPanel>

                <lvc:PieChart Series="{Binding Series}" Height="300"/>
                <lvc:PieChart Series="{Binding Series}" Height="300"/>
                <lvc:PieChart Series="{Binding Series}" Height="300"/>
                <lvc:PieChart Series="{Binding Series}" Height="300"/>
            </UniformGrid>
        </ScrollViewer>
    </Grid>
</UserControl>
